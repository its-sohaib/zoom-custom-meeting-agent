{
  "name": "zoom-meeting-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "POC: join a Zoom meeting from a meeting link via Playwright",
  "scripts": {
    "join": "node src/join-zoom.mjs",
    "postinstall": "playwright install chromium"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "playwright": "^1.49.0"
  }
}
